

struct Reciept {

  import static function Enable();
  import static function Disable();
  
  import static bool IsEnabled();
    
  /**
   * Test the selected items on a hotspot. Which means that the user 
   * already selected several items and then clicked on one of
   * the hotspots with action.
   *
   */
  import static  function TestSelectedItemsOn(int hotspotId);
  
  
};